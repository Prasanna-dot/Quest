<header>
    <nav>
        <div class="logo_div">
            <%= image_tag("quest_logo.png", :alt => "quest_logo", :id => "logo_img") %>
        </div>
        <div class="menu_div">
            <ul>
                <li>Interview Q&A</li>
                <li>Quize</li>
                <li>Evaluation</li>
            <% if current_user %>
                <li><%= link_to "Log Out", logout_path %></li>
            <% else %>
                <li><%= link_to "Log In", login_path %></li>
            <% end %>
            </ul>
        </div>
    </nav>
</header>